import { default as FlightList } from './pages';

const FlightsFeature = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <FlightList />
    </div>
  );
};

export default FlightsFeature;
